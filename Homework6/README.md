### Needs the following packages installed: ggplot2, extraDistr
### Please use the following commands in Rstudio to install the necessary packages

install.packages("ggplot2") <br>
install.packages("extraDistr")
